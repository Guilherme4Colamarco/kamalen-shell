# SPECIFICATION - Alphonso Improvements

**Project:** Alphonso (MangoWM Rice/Dotfiles)
**Goal:** Improve code quality, documentation, testing, and maintainability

---

## 1. Current State Analysis

### Files Analyzed
- `iris.py` (846 lines) - Color extraction script
- `Colors.qml` (242 lines) - Color singleton
- `UIState.qml` - Global state management
- `shell.qml` (244 lines) - Shell entry point
- `install.sh` (194 lines) - Installation script
- Various QML widgets (Bar, Dashboard, Launcher, etc.)

### Issues Identified

#### Critical (Must Fix)
1. **iris.py**: No type hints, no docstrings, no error handling in color extraction
2. **install.sh**: No rollback mechanism, no dependency verification
3. **No tests**: Zero test coverage for any component
4. **No CI/CD**: No automated quality checks

#### High Priority
1. **Code duplication**: Multiple QML files repeat similar patterns
2. **Hardcoded paths**: Many paths are hardcoded instead of configurable
3. **No logging**: Debugging is difficult without structured logging
4. **No configuration validation**: Settings can be invalid without feedback

#### Medium Priority
1. **Documentation**: README is good but lacks API docs for iris.py
2. **Performance**: Some QML animations could be optimized
3. **Accessibility**: No keyboard navigation improvements
4. **Error messages**: User-facing errors are unclear

#### Low Priority
1. **Code style**: Inconsistent formatting across files
2. **Dead code**: Some unused functions/variables
3. **Comments**: Many sections lack comments

---

## 2. Requirements

### 2.1 Functional Requirements

| ID | Requirement | Priority | Acceptance Criteria |
|----|-------------|----------|---------------------|
| FR-001 | Add type hints to iris.py | Must | All functions have type hints |
| FR-002 | Add docstrings to iris.py | Must | All public functions documented |
| FR-003 | Add error handling to iris.py | Must | Graceful failure with user feedback |
| FR-004 | Create unit tests for iris.py | Must | >80% coverage |
| FR-005 | Add validation to install.sh | Must | Verify dependencies before install |
| FR-006 | Create CI/CD pipeline | Should | GitHub Actions for lint/test |
| FR-007 | Add configuration validation | Should | Invalid settings show warnings |
| FR-008 | Improve error messages | Should | Clear, actionable error messages |
| FR-009 | Add logging to iris.py | Should | Structured logging for debugging |
| FR-010 | Create API documentation | Could | Auto-generated docs for iris.py |

### 2.2 Non-Functional Requirements

| ID | Requirement | Target |
|----|-------------|--------|
| NFR-001 | Test coverage | >80% |
| NFR-002 | Linting | ruff, mypy pass |
| NFR-003 | Security | bandit no critical issues |
| NFR-004 | Documentation | All public APIs documented |

---

## 3. Architecture

### 3.1 Component Structure

```
Alphonso/
├── .config/
│   ├── quickshell/
│   │   ├── iris/iris.py          # Color extraction
│   │   ├── Colors.qml            # Color singleton
│   │   ├── UIState.qml           # Global state
│   │   └── *.qml                 # Widgets
│   ├── mango/config.conf         # WM config
│   └── kitty/kitty.conf          # Terminal config
├── tests/
│   ├── test_iris.py              # Unit tests
│   └── test_install.sh           # Install script tests
├── docs/
│   └── API.md                    # API documentation
├── .github/workflows/
│   └── ci.yml                    # CI pipeline
└── install.sh                    # Installation script
```

### 3.2 Testing Strategy

- **Unit tests**: iris.py functions (color conversion, extraction, caching)
- **Integration tests**: install.sh dependency checks
- **Linting**: ruff for Python, qmllint for QML

---

## 4. Deliverables

- [ ] Type hints added to iris.py
- [ ] Docstrings added to iris.py
- [ ] Error handling improved in iris.py
- [ ] Unit tests created (>80% coverage)
- [ ] install.sh validation improved
- [ ] CI/CD pipeline configured
- [ ] Configuration validation added
- [ ] Error messages improved
- [ ] Logging added to iris.py
- [ ] API documentation generated

---

## 5. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Breaking existing functionality | Medium | High | Run tests before/after changes |
| QML syntax errors | Low | Medium | Use qmllint, test each change |
| Install script failures | Medium | Medium | Add rollback, test on clean system |
| Performance regression | Low | Medium | Benchmark before/after |

---

*Generated by swe-agent Spec Node*